version: v1
managed:
  enabled: true
plugins:
  # This will invoke protoc-gen-es and write output to src/gen
  - name: es
    out: src/protos
    opt: 
      # Add more plugin options here
      - target=js+ts+dts