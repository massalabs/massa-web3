import 'mocha';
import { assert } from 'chai';
import { Test } from 'mocha';

describe('Test Suite - web3 sdk', () => {
  describe('Test case - Massa Wallets', () => {
    it('Serialize & Deserialize.', () => {
      assert(true);
    });

    it('Send a basic payload.', async () => {
      assert(true);
    });
  });
});
